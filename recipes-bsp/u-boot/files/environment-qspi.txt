recovery=setenv bootargs "${bootargs_console} boot_medium=qspi rw rootwait quiet loglevel=0 ${bootargs_hwtest}"; sf probe 0:0 && sf read ${kernel_addr_r} ${kernel_addr} ${kernel_size} && sf read ${fdt_addr_r} ${fdt_addr} ${fdt_size} && booti ${kernel_addr_r} - ${fdt_addr_r}
