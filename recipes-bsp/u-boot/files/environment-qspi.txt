recovery=setenv bootargs "${bootargs_console} boot_medium=qspi rw rootwait quiet loglevel=0 ${bootargs_hwtest}"; sf probe 0:0 && sf read ${loadaddr} ${kernel_addr} ${kernel_size} && bootm ${loadaddr}
