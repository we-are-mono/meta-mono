header:
  version: 11
  includes:
    - common.yaml

machine: gateway-dk
distro: recovery

repos:
  meta-mono-bsp:
    path: ./meta-mono-bsp

target:
  - recovery-image
  - firmware-image

local_conf_header:
  recovery: |
    TMPDIR = "${TOPDIR}/tmp-recovery"
